import React, { Component } from 'react';

export default class RightBarLayout extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
    
        return(<right_bar>
        </right_bar>);
    }
}